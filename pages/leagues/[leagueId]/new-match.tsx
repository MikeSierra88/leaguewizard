import React from 'react';
import { Container, Typography } from '@mui/material';

const NewMatchPage = () => {
  return (
    <Container sx={{ marginTop: '1rem' }}>
      <Typography variant="h5">Add new match placeholder</Typography>
    </Container>
  );
};

export default NewMatchPage;
